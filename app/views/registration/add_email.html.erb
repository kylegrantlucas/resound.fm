<h1>Missing email :(</h1>
<form role="form" method='POST' id="email-form" class="form" data-validate="parsley">
  <% if current_user.errors.any? %>
  <div class="error_explanation">
    <% current_user.errors.full_messages.each do |msg| %>
      <h4><%= msg %></h4>
    <% end %>
  </div>
  <% else %>
  <h4>Hmm&hellip; Looks like we're missing your email address.<br/><br/>We'll need your email before you can claim your username.</h4>
  <% end %>
  <div class="form-group input-group input-group-lg">
    <span class="input-group-addon">Your email</span>
    <input type="text" class="form-control" type="email" data-parsley-type="email" data-trigger='change' name='email' spellcheck="false" autofocus placeholder="<%= current_user.email || "me@email.com" %>">
  </div>
  <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>" />
  <button type="submit" class="btn btn-danger pull-right">You got it</button>
</form>
